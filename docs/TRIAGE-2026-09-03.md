# Probably Weather — shipped fix report

**Date:** 03/09/2026 (SAST)
**Production code commit:** `c87956d31d550450208dda3a6c04f85919fa0012`
**Scope:** approved Privacy route, first-visit install banner, duplicate-assets image requests, and sealed `.env` remediation.

## `.env` — Al's exact steps (do this first)

### 1. Remove the project from OneDrive sync

**Recommended — move only this project:**

1. Close Codex, VS Code, terminals, and any preview server using Probably Weather.
2. In File Explorer, create `C:\dev` if it does not exist.
3. Move the complete folder `C:\Users\27741\OneDrive\Desktop\Probably weather new\probably-weather-new-c` to `C:\dev\probably-weather-new-c`. Move the whole folder so the hidden `.git` directory and the ignored `.env` travel together.
4. Re-open `C:\dev\probably-weather-new-c` in Codex. In PowerShell, run:

   ```powershell
   git -C "C:\dev\probably-weather-new-c" rev-parse --show-toplevel
   git -C "C:\dev\probably-weather-new-c" check-ignore -v .env
   git -C "C:\dev\probably-weather-new-c" ls-files --error-unmatch .env
   ```

   The first command must print the new `C:\dev\...` path. The second must print the `.gitignore` rule. The third must end with an error saying the path is not known to Git; that error is the desired result.
5. Open OneDrive in the browser. Confirm the old Probably Weather folder is gone, then delete any old project copy from the OneDrive recycle bin. If the folder was ever shared, remove the share as well.
6. Do not copy `.env` back into any OneDrive, Dropbox, Google Drive, or other synced folder.

**Alternative — stop syncing the entire Windows Desktop:** OneDrive does not provide a safe “do not upload this one subfolder” switch. “Choose folders” only controls which cloud folders are downloaded and does not protect a local secret from upload. To keep this project on Desktop, click the OneDrive cloud icon → **Settings** → **Sync and backup** → **Manage backup** → turn **Desktop** off → **Stop backup**, then move the project to the non-OneDrive Desktop path Windows creates and confirm its path no longer contains `\OneDrive\`. This affects every Desktop item, so moving only the project to `C:\dev` is the safer option.

### 2. Git protection already confirmed

- **[verified]** `.gitignore:7` is exactly `.env`; `.gitignore:6` also covers `.env*.local`.
- **[verified]** `git ls-files --error-unmatch .env` found no tracked file, `git status --short -- .env` was blank, and `git check-ignore -v .env` resolved to `.gitignore:7`.
- **[verified]** No `.env` contents were opened or changed during this work.

### 3. Rotate the credentials Al finds in `.env`

**[verified]** The current server code reads the five secret names below. **[inferred]** Because `.env` remained sealed, this report cannot claim which of them are actually present; Al should rotate every listed name he sees when he opens the file himself.

Do one provider at a time. Create the replacement first where the provider allows two active keys, update Vercel, redeploy and test, then revoke the old key.

1. `WEATHERAPI_KEY` — sign in to WeatherAPI, open the account/API-key page, and click **Regenerate** beside the key. WeatherAPI documents that exact control in its [authentication documentation](https://www.weatherapi.com/docs/). Copy the replacement into Vercel before using the old value anywhere else.
2. `PIRATE_WEATHER_KEY` — sign in to the [Pirate Weather Apiable portal](https://pirate-weather.apiable.io/), open the subscribed Forecast API, and request/create a replacement key. If the portal exposes no regenerate or second-key control, email `mail@pirateweather.net` and ask for the current key to be revoked and reissued; the [official API documentation](https://docs.pirateweather.net/en/latest/API/) identifies that portal and confirms the key is secret and unique per user.
3. `TOMORROWIO_API_KEY` — sign in to Tomorrow.io, open **Development** / the Dashboard access-key area, and create a new key if that control is available. Tomorrow.io says the default private token lives on the Dashboard and additional keys may require the account manager; if no self-service replacement is shown, email `support@tomorrow.io` and ask them to replace the private access key. See [Tomorrow.io Access Keys](https://docs.tomorrow.io/reference/api-authentication).
4. `LOCATIONIQ_TOKEN` — sign in to the LocationIQ User Dashboard → **API Access Tokens**. Create a replacement token, label it “Probably Weather”, and apply the appropriate IP/referrer restriction. On a free plan, where only one token is available, use the token's **Reissue** control; if it is absent, contact LocationIQ support before revoking the working value. LocationIQ's [token guide](https://help.locationiq.com/support/solutions/articles/36000216109-how-to-generate-more-public-access-tokens-api-keys-) and [security guide](https://docs.locationiq.com/docs/authentication) describe this screen and recommend frequent reissue.
5. `UPSTASH_KV_REST_API_TOKEN` — sign in to the Upstash Console, select the Redis database used by Probably Weather, then reset the database password/credentials. Upstash states that this revokes both Standard and Read Only REST tokens. Open **Connect** → **REST** and copy the new Standard token into `UPSTASH_KV_REST_API_TOKEN`. Keep `UPSTASH_KV_REST_API_URL` paired with the same database; its URL normally does not need rotation. See the [Upstash REST API security documentation](https://upstash.com/docs/redis/features/restapi).

For each replacement, go to Vercel → **Probably Weather** → **Settings** → **Environment Variables** → find the exact variable name → **Edit** → paste the new value → select every environment that currently uses it (Production, Preview and/or Development) → **Save**. Redeploy, confirm the provider-backed feature works, then invalidate the old credential. Vercel confirms that variable changes affect only new deployments in its [secret-rotation guide](https://vercel.com/docs/environment-variables/rotating-secrets).

Do **not** rotate `MET_USER_AGENT`, `PW_ERROR_ALLOWED_ORIGINS`, `UPSTASH_KV_REST_API_URL`, or `VERCEL_GIT_COMMIT_SHA`: the first three are configuration/endpoint values rather than secret credentials, and Vercel supplies the last one automatically.

### 4. What I would do after Al rotates them

**[inferred]** I would run a value-redacted secret/history scan, confirm `.env` is still ignored and absent from every Git object, then production-smoke weather aggregation, LocationIQ search/reverse lookup, and Upstash-backed rate limiting. I would check Vercel function logs for provider authentication failures without printing any credential and confirm all old credentials are revoked only after the new production deployment is healthy.

## 1. Approved Privacy page

- **Diagnosis — [verified]:** `privacy.html` already contained Al's approved policy. Production served `/privacy.html` with HTTP 200 but `/privacy` returned 404 because `vercel.json` had rewrites for `/share` and `/install` only.
- **Fix — [verified]:** `vercel.json:32–33` now rewrites `/privacy` to `/privacy.html`. `privacy.html` was not edited.
- **Content proof — [verified]:** local and live `/privacy` normalized SHA-256 are both `4dd4d70fa783c492a64f32f043cf3615f3a7253edb00f914c6b4d9d35f1b844f`. Live title is `Privacy Policy · Probably Weather`, the page contains the approved POPIA statement and `howzit@probablyweather.co.za`, and both `/privacy` and `/privacy.html` return `200 text/html; charset=utf-8`.
- **Incidental console closure — [verified]:** the approved static page caused Chrome to request `/favicon.ico`. To keep the page byte-for-byte unchanged and meet the zero-asset-error gate, `vercel.json:36–37` routes that request to the existing `/assets/favicon-32.png`; live response is `200 image/png`.
- **Screenshot — [verified]:** [live approved Privacy page at 390×844](../output/playwright/pw-fix-2026-09-03/after-privacy-live-390x844.png).

## 2. First-visit install banner at 390×844

- **Before — [verified]:** on a pristine live production visit at scroll position 0, the fixed banner occupied `y=626.04–694.00`, covering the condition description (`y=613.92–638.87`) and source-agreement line (`y=679.81–695.81`). [Before screenshot](../output/playwright/pw-fix-2026-09-03/before-banner-live-390x844.png).
- **Fix — [verified]:** `assets/install.js:561–564` arms the existing 1.5-second eligibility timer only after the document's first scroll. The footer's explicit Install action is unchanged.
- **After — [verified]:** a new pristine live visit to production at 390×844 was held for five seconds at `scrollY=0`; the banner remained `hidden=true`, `visible=false`, with a zero-sized rectangle. The temperature, description and agreement line were all visible. [After screenshot](../output/playwright/pw-fix-2026-09-03/after-banner-live-390x844.png).

## 3. Five `/assets/assets/` image failures

- **Diagnosis — [verified]:** `index.html` put relative `assets/images/...` values into `--hero-url`, while that custom property is consumed by `/assets/app.css`. The browser therefore resolved the CSS URL from `/assets/`, producing `/assets/assets/...`.
- **Fix — [verified]:** `index.html:326–334` sanitizes the stored/app path, resolves it with `new URL(safe, document.baseURI).href`, then writes the absolute URL to `--hero-url`. This repairs both fresh defaults and relative paths already saved by older app versions. The inline-script CSP hashes were regenerated and verified.
- **The five requests and their corrected live result — [verified]:**

  1. `/assets/assets/images/bg/default.jpg` → `/assets/images/bg/default.jpg` → `200 image/jpeg`
  2. `/assets/assets/images/bg/default.jpg` → `/assets/images/bg/default.jpg` → `200 image/jpeg`
  3. `/assets/assets/images/bg/default.jpg` → `/assets/images/bg/default.jpg` → `200 image/jpeg`
  4. `/assets/assets/images/bg-canonical/1c536cb795d498ce4c87a4e34119a6db92753d01cdd22e6d9203f2115bdc36ad.webp?v=20260718-p1` → the same path with one `/assets/` prefix → `200 image/webp`
  5. `/assets/assets/images/bg-canonical/1c536cb795d498ce4c87a4e34119a6db92753d01cdd22e6d9203f2115bdc36ad.webp?v=20260718-p1` → the same path with one `/assets/` prefix → `200 image/webp`

- **Current-weather proof — [verified]:** the final clean browser visit selected `/assets/images/bg-canonical/2e94d50f6e9d4da4e712b82174c98b9f13768b1f32ee12cdd53b7d0e37f6709e.webp?v=20260718-p1`; both `#bgImg.currentSrc` and computed `--hero-url` held that single-prefix URL, which returned `200 image/webp`.
- **Console/network proof — [verified]:** the final pristine app visit recorded `0` errors, `0` warnings, and an empty resource list for URLs containing `/assets/assets/`. The subsequent live Privacy visit also recorded `0` errors and `0` warnings.

## Gates and deployment

- **[verified]** `npm test` → `88 passed (88)` test files; `21,747 passed (21,747)` tests.
- **[verified]** `npm run build` → background budget passed; `1,008/1,008` image slots byte-equivalent; `13/13` service-worker precache paths valid; build completed cleanly.
- **[verified]** `node scripts/generate-csp.mjs` → `vercel.json CSP is in sync (18 hashes, Content-Security-Policy-Report-Only).`
- **[verified]** `git diff --exit-code -- privacy.html` → exit 0.
- **[verified]** GitHub push to `main` succeeded. Production `/api/version` reported `c87956d31d550450208dda3a6c04f85919fa0012` before the final browser pass.
- **[verified]** One cold full-suite run during the routing-only stage exceeded the image-manifest test's 5-second timeout while hashing 1,008 OneDrive files. The unchanged immediate rerun passed 88/88, every later full suite passed, and the build's equivalent 1,008/1,008 byte check passed throughout.

## Commits

- `9ffc6e4da6c6074a3d78a376127dd102d8cd121c` — route the approved Privacy page.
- `0f6c79ce9de5795f245b11aa28a7215572ed1eea` — defer the install banner until first scroll.
- `c03ed4d458313254084f7ee669fe1ec4dbd5e206` — resolve hero images from the document root and refresh CSP hashes.
- `c87956d31d550450208dda3a6c04f85919fa0012` — serve the existing favicon on static pages so the final console is clean.

## Put it to rest

This lane is put to rest when Al has moved the repository out of OneDrive (or disabled Desktop backup), rotated every applicable credential above, updated and redeployed the matching Vercel variables, and handed the new deployment back for a redacted provider smoke. The code side is already closed: the approved policy is served unchanged at `/privacy`, the first unscrolled forecast view is unobstructed at 390×844, all formerly duplicated asset paths resolve with one `/assets/` prefix, the live app and Privacy page have clean consoles, and the tested code is on production.
