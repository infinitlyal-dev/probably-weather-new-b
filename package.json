{
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "build": "node scripts/verify-bg-image-budget.mjs && node scripts/build.mjs",
    "images:recompress": "node scripts/recompress-bg-images.mjs",
    "images:verify": "node scripts/verify-bg-image-budget.mjs",
    "copy:generate": "node scripts/generate-copy-splits.mjs",
    "profile:particles": "node scripts/profile-particles.mjs"
  },
  "devDependencies": {
    "esbuild": "^0.28.0",
    "playwright": "^1.58.2",
    "vitest": "^4.1.5"
  },
  "dependencies": {
    "@upstash/ratelimit": "^2.0.8",
    "@upstash/redis": "^1.38.0",
    "@vercel/analytics": "^2.0.1",
    "@vercel/functions": "^3.4.3",
    "satori": "^0.25.0",
    "sharp": "^0.34.5"
  }
}
